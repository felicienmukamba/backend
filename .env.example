# 🌍 Environment Definition
NODE_ENV=development
PORT=3000

# 🐘 Database (Prisma)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/milele_db?schema=public"

# 🔐 JWT Authentication
# Generate strong secrets: openssl rand -base64 32
JWT_SECRET="changeme_min_32_chars_long_secret_key_here"
JWT_REFRESH_SECRET="changeme_min_32_chars_long_refresh_secret_here"
JWT_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"

# 🌐 CORS Configuration
# Comma separated list of allowed origins
CORS_ORIGINS="http://localhost:3000,http://localhost:3001,https://milele.app"

# 📝 Logging
# Options: error, warn, info, debug, trace
LOG_LEVEL="info"

# 📧 SMTP Configuration (Optional)
# SMTP_HOST="smtp.example.com"
# SMTP_PORT=587
# SMTP_USER="user@example.com"
# SMTP_PASSWORD="password"
# SMTP_FROM="noreply@milele.app"

# 🏛️ DGI / MCF Integration (Optional - RDC)
# DGI_API_URL="https://api.dgi.gouv.cd"
# DGI_API_KEY="your_dgi_api_key"
# DGI_TIMEOUT=30000

# 🚦 Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# 📂 File Uploads
MAX_FILE_SIZE=5242880
UPLOAD_PATH="./uploads"

# 👑 Super Admin Credentials
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="your_secure_password"
